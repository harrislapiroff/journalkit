# Keep the virtualenv at ./.venv rather than in Poetry's global cache.
# Everything else in this repo — the `pagekit-dev doctor` checks, the paths in
# .claude/skills/run-pagekit/SKILL.md, and the permission allowlist in
# .claude/settings.json — refers to .venv/bin/, so this must stay true.
[virtualenvs]
in-project = true
