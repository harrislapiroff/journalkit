# Keep the virtualenv at ./.venv rather than in Poetry's global cache.
# Everything else in this repo — `tools/dev.py doctor`, the paths in
# .claude/skills/run-journalkit/SKILL.md, and the permission allowlist in
# .claude/settings.json — refers to .venv/bin/, so this must stay true.
[virtualenvs]
in-project = true
